[project]
name = "agent-service"
version = "0.3.0"
description = "FastAPI + LangGraph + Ollama case/ops agent (propose-only)"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "langgraph>=1.2.11",
    "psycopg[binary,pool]>=3.3.5",
    "pydantic-settings>=2.15.0",
    "uvicorn[standard]>=0.52.4",
]
